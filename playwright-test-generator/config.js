import path from 'path';

// The default basePath is used as the root directory for all generated test files.
const defaultBasePath = path.resolve('./');

export const config = {
  basePath: defaultBasePath,
};